#pragma once
#include <vector>

int EnterPositiveNumber(int minLimit = 0);
std::vector<int> GetAllNumbers(int min, int max);
void ShowPrimeNumbers(const std::vector<int>& numbers);
void ShowPrimeNumber(int number);
bool IsPrime(int number);
